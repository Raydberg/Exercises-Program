/**
 * Se llama a la función contar, que define una variable contador con el valor 0 y devuelve una función anónima.
 * La función anónima incrementa el valor de contador y lo registra en la consola.
 * La constante incrementar se asigna a la función anónima devuelta por contar.
 * Cada vez que se llama a incrementar, se incrementa y se muestra el valor de contador.
 * El resultado de ejecutar este código será
 * 1
 * 2
 * 3
 * 4
 */