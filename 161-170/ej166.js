// Defining the variable usuario with var
var usuario = { nombre: 'GitHub Copilot' };

function isUserValid() {
    if (usuario.nombre === 'GitHub Copilot') {
        console.log('Éxito: El nombre del usuario es correcto.');
    } else {
        console.log('Error: El nombre del usuario es incorrecto.');
    }
}

isUserValid();

let usuario = { nombre: 'GitHub Copilot' };
isUserValid();

const usuario = { nombre: 'GitHub Copilot' };
isUserValid();